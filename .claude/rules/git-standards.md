# Git 規則

- Commit 格式：`<類型>: <簡短描述>`（繁體中文）
- 類型：feat、fix、refactor、test、docs、chore
- 禁止 `--force` push 到 main
- 禁止 `--no-verify` 繞過 pre-commit hooks
- 分支策略：main（穩定）、feature/<名稱>、fix/<名稱>、refactor/<名稱>
